import "../styles.css";
import React from 'react';

const InputBox = ({type}) => {

    return (
        <input
            className="inputbox"
            type={type}
        >

        </input>
    )
}

export default InputBox;